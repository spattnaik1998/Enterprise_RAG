# ─────────────────────────────────────────────────────────────────────────────
# Vercel production requirements (serving layer only)
# Excludes: faiss-cpu, rank-bm25, tiktoken, nltk, langdetect, faker, mcp,
#           arxiv, wikipedia-api, feedparser  (Phase I/II tools not needed at runtime)
# ─────────────────────────────────────────────────────────────────────────────

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP & Async
httpx>=0.27.0
aiofiles>=23.0.0

# Utilities
loguru>=0.7.0
pyyaml>=6.0.0
tenacity>=8.0.0
orjson>=3.9.0

# AI APIs
openai>=1.0.0
anthropic>=0.25.0

# Supabase (replaces FAISS + JSONL)
supabase>=2.3.0

# Observability
langsmith>=0.1.0

# Web
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Numpy (still needed for query embedding normalisation)
numpy>=1.26.0
